import React from 'react';
import { Link } from 'react-router-dom';

const List = ({data}) => {
    return (
        <div>
        <Link to="/universal">
            Universal Studios Singapore
          </Link>
        </div>
    );
}

export default List;
